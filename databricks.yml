
bundle:
  name: commodities_prices

include:
  - resources/*.yml
  
targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-b1ae0495-fc55.cloud.databricks.com
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}


